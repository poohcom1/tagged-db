{
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "server",
    "client",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm run dev --workspace shared\" \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "dev:client": "concurrently -k \"npm run dev --workspace shared\" \"npm run dev --workspace client\"",
    "build:client": "npm run build --workspace shared && npm run build --workspace client",
    "build:server": "npm run build --workspace shared && npm run build --workspace server",
    "start:server": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
