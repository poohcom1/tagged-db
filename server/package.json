{
  "name": "tagged-db-server",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node src/index.js",
    "dev": "nodemon --exec tsx ./src/server.ts",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch"
  },
  "dependencies": {
    "@app/shared": "file:../shared",
    "@fastify/cors": "^11.2.0",
    "fastify": "^5.7.4"
  },
  "devDependencies": {
    "fastify-cli": "^7.4.1",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
